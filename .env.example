PORT=5000
NODE_ENV=development

# Database Configuration (Sequelize)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=baznas_batam
DB_PORT=3306

# JWT Configuration
# WAJIB: Ganti dengan random string yang kuat, minimal 32 karakter
# Generate dengan: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=GANTI_DENGAN_STRING_RANDOM_YANG_KUAT_MINIMAL_32_KARAKTER
JWT_EXPIRES_IN=1d

# CORS - Daftar origin yang diizinkan, pisahkan dengan koma
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# Seed Superadmin (opsional, ada default jika tidak diset)
# Ganti dengan credentials yang kuat di production!
SEED_ADMIN_USERNAME=baznasbatam01
SEED_ADMIN_PASSWORD=GANTI_PASSWORD_YANG_KUAT
